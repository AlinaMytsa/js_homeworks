'use strict';

(function () {
  const form = document.querySelector('[data-todo-form]');

  const inputs = form.querySelectorAll('input, textarea');
  const arrayOfInputs = Array.from(inputs);
  const newArrayOfInputs = arrayOfInputs.map();
}());
